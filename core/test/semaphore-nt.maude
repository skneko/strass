load ../src/main.maude
load ../src/io.maude
load in/semaphore-nt.maude

mod EMPTY is
endm

mod TEST is
    pr STRASS .
    pr IO .
endm

erew writeToFile("out/semaphore-nt-fixed.maude", fix('SEMAPHORE, 'EMPTY, readFileToEnd("in/semaphore-nt.constraints"))) .

quit