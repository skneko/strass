load semaphore.maude	---Load files containing the original definitions.

***(
	Generated by STRASS -- Daniel Galán <dagapas@vrain.upv.es>, 2021
)

--- Notice: extended by STRASS, not the original definition
mod EMPTY is
	including BOOL .
	protecting EXT-BOOL .
endm

smod SEMAPHORE-STRAT is
	protecting SEMAPHORE .
	protecting EMPTY .
	strat GlobalState : @ GlobalState .
	strat GlobalState! : @ GlobalState .
	strat GlobalState* : @ GlobalState .
	strat GlobalState+ : @ GlobalState .
	strat PSet : @ PSet .
	strat PSet! : @ PSet .
	strat PSet* : @ PSet .
	strat PSet+ : @ PSet .
	strat PState : @ PState .
	strat PState! : @ PState .
	strat PState* : @ PState .
	strat PState+ : @ PState .
	strat Semaphore : @ Semaphore .
	strat Semaphore! : @ Semaphore .
	strat Semaphore* : @ Semaphore .
	strat Semaphore+ : @ Semaphore .
	strat f0 : @ PSet .
	strat f1 : @ GlobalState .
	strat f2 : @ Process .
	strat f3 : @ GlobalState .
	sd GlobalState := (f1) ; ((f3) ; ((f0) ; (f2))) .
	sd GlobalState! := (GlobalState) ; (((all) ; (GlobalState)) !) .
	sd GlobalState* := (GlobalState) ; (((all) ; (GlobalState)) *) .
	sd GlobalState+ := (GlobalState) ; (((all) ; (GlobalState)) +) .
	sd PSet := (f0) ; (f2) .
	sd PSet! := (PSet) ; (((all) ; (PSet)) !) .
	sd PSet* := (PSet) ; (((all) ; (PSet)) *) .
	sd PSet+ := (PSet) ; (((all) ; (PSet)) +) .
	sd PState := idle .
	sd PState! := (all) ! .
	sd PState* := (all) * .
	sd PState+ := (all) + .
	sd Semaphore := idle .
	sd Semaphore! := (all) ! .
	sd Semaphore* := (all) * .
	sd Semaphore+ := (all) + .
	sd f0 := not(amatch p N:Nat{PS:PState} p N:Nat{PS':PState} s.t. false = false) .
	sd f1 := not(amatch B:Bool || p N:Nat{critical} p M:Nat{critical} s.t. false = false) .
	sd f2 := not(amatch p N:Nat{idle} s.t. N:Nat > 0 = false) .
	sd f3 := not(amatch false || p N:Nat{entering} PS:PSet s.t. true = false) .
endsm
